import { Tabs } from "./Tabs";

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  component: Tabs,
  title: 'Base/Tabs',
  tags: ['autodocs']
}

export const Sample = {
  args: {
    varient:'default'
  },
};
